[project]
name = "pil-314t-uv"
version = "0.1.0"
description = "Test project to demonstrate PIL.Image import crash in Python 3.14t"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.0.0",
]
